# Cortexbias: Technical Definition

    ## Formal Definition
    Systematic decision-making deviations arising from the neural architecture and dynamic balance between prefrontal cortex deliberative processes and limbic system emotional responses. Cortexbias manifests when the relative activation levels of these brain regions produce consistent patterns of economic choice that deviate from normative rationality predictions.

    ## Conceptual Background
    Cortexbias is used here as an operational concept rather than a rhetorical label. It addresses neural architecture imbalance in economic decision making. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. It addresses neural architecture imbalance in economic decision making. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. It addresses neural architecture imbalance in economic decision making. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. It addresses neural architecture imbalance in economic decision making. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    ## Theoretical Framework
    Cortexbias is used here as an operational concept rather than a rhetorical label. The theoretical mapping treats the construct as a latent index inferred from observed proxies. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. The theoretical mapping treats the construct as a latent index inferred from observed proxies. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. The theoretical mapping treats the construct as a latent index inferred from observed proxies. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. The theoretical mapping treats the construct as a latent index inferred from observed proxies. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    ## Practical Applications
    Cortexbias is used here as an operational concept rather than a rhetorical label. Applications are intentionally practical and reproducible. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. Applications are intentionally practical and reproducible. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. Applications are intentionally practical and reproducible. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    ## Relationship to Existing Concepts
    Related concepts include: Affective bias, Cognitive control imbalance, Loss aversion dynamics.
    The main distinction in this repository is reproducible operationalization with consistent variable and index definitions.

    ## Measurement and Empirics
    Cortexbias is used here as an operational concept rather than a rhetorical label. Empirical use should combine reliability, predictive validity, and sensitivity analysis. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. Empirical use should combine reliability, predictive validity, and sensitivity analysis. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    Cortexbias is used here as an operational concept rather than a rhetorical label. Empirical use should combine reliability, predictive validity, and sensitivity analysis. The mechanism focus is choice distortion when reactive channels dominate deliberative control, which can be represented with explicit channels: prefrontal_control, limbic_reactivity, stress_load, ambiguity_level, decision_time_ms, loss_aversion_ratio, risk_premium_gap. This repository couples concept definition with executable implementation so assumptions remain inspectable. In practice, the framework supports comparative statics, scenario simulation, and policy diagnostics. Typical applications include consumer finance diagnostics, workplace decision redesign, and mechanism-aware experiments. Because the index logic is transparent, researchers can vary weights and test robustness across model specifications.

    ## References
    1. Acemoglu, D., Autor, D., and Johnson, S. (2024). Technology diffusion and economic adjustment.
2. Athey, S. and Luca, M. (2019). Economists and big data.
3. Goldfarb, A., Tucker, C., and Wang, Y. (2024). AI economics and industrial dynamics.
4. Hall, R. and Kudlyak, M. (2021). Labor market slack and dynamics.
5. Mullainathan, S. and Spiess, J. (2017). Machine learning in applied econometrics.
6. Calvano, E. et al. (2020). Algorithmic pricing and market outcomes.
7. Autor, D., Mindell, D., and Reynolds, E. (2022). The work of the future.
8. Stock, J. and Watson, M. (2021). Inflation dynamics and expectations.
